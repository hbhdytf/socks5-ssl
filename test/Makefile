all:
	gcc -g -lssl -lcrypto -o vpn_client vpn_client.c
	gcc -g -lssl -lcrypto -o vpn_server vpn_server.c
	gcc -g ssl-server.cpp -lssl -lcrypto -o ssl-server
	gcc -g ssl-client.cpp  -lssl -lcrypto -o ssl-client
